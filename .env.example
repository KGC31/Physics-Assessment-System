# Google OAuth (set via: npx ampx sandbox secret set GOOGLE_CLIENT_ID)
# GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are Amplify sandbox secrets, not .env vars.

# After `npx ampx sandbox`, amplify_outputs.json is regenerated automatically.
